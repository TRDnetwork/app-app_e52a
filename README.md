This is a static personal portfolio site with no backend requirements. The schema files are placeholders to satisfy pipeline expectations, but no database tables, storage buckets, or real-time functionality are used. Content is fully client-side rendered using React + Vite, with simulated form submission and hardcoded project data. Responsive layout and animations are handled via Tailwind CSS and Framer Motion.